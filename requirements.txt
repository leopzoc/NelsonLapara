# ══════════════════════════════════════════════════════════════════════
#  ONIX — Dependencies
# ══════════════════════════════════════════════════════════════════════

# ── RPi 5 Runtime (install on Pi) ───────────────────────────────────
# sudo pip install -r requirements.txt
# (sudo required for rpi_ws281x PWM DMA access)

numpy>=1.24
sounddevice>=0.4.6
soundfile>=0.12
scipy>=1.11
transformers>=4.40
onnxruntime>=1.17
torch>=2.1
onnx>=1.15
adafruit-blinka>=8.0
adafruit-circuitpython-neopixel-spi>=1.0
rpi-lgpio>=0.4
pygame>=2.5

# ── Training Only (GPU machine, not RPi) ────────────────────────────
# pip install torchaudio kagglehub tqdm onnxscript

# torchaudio>=2.1
# kagglehub
# tqdm
# onnxscript
